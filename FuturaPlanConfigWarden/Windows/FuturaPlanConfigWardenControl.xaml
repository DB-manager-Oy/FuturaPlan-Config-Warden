<UserControl x:Class="FuturaPlanConfigWarden.Windows.FuturaPlanConfigWardenControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        
        <Label Grid.Column="0" Content="Database:" VerticalAlignment="Center"/>
        <ComboBox Grid.Column="1" SelectedItem="{Binding Path=SelectedDatabase}" ItemsSource="{Binding Path=DatabaseList}" Margin="5"/>
        
        <Label Grid.Column="0" Grid.Row="1" Content="Override: "/>
        <CheckBox IsChecked="{Binding Path=OverrideConnectionString, Mode=TwoWay}" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5"/>

        <Button Command="{Binding Path=RefreshDatabaseListCommand}" Content="Refresh Databases" Grid.Row="2" Margin="5"/>
    </Grid>
</UserControl>
